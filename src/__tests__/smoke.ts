import {SearchPage } from "../models/SearchPage";

it("Verify Bing Search", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 2", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 3", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 4", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 5", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 6", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 7", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 8", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 9", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 10", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });

  it("Verify Bing Search 11", async () => {
  
    const searchPage = new SearchPage(page);
    await searchPage.navigate();
    await searchPage.search('i am learning playwright');
    await searchPage.verifyResult();
  });